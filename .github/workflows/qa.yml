name: qa

on: push
#  push:
#    paths:
#      - 'src/*'

jobs:
  qa:
    runs-on: ubuntu-latest
    name: qa
    steps:
      - uses: actions/checkout@v1
      - uses: actions/setup-node@v1
        with:
          node-version: '12.x'
      - run: npx install-peerdeps --global eslint-config-airbnb-base
      - run: eslint ./src
