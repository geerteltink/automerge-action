name: test

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  test:
    name: ci/test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: npm ci
      - run: npm test
      - run: npm build
